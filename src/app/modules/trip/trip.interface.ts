export type ITripFilters = {
  searchTerm?: string;
  tripId?: string;
  startDate?: string;
  endDate?: string;
  from?: string;
  to?: string;
  minValue?: string;
  maxValue?: string;
  costing?: string;
  vehicleId?: string;
  driverId?: string;
  partyId?: string;
};
